import { ActivityTemplate } from './activity-template.model';

describe('ActivityTemplate', () => {
  it('should create an instance', () => {
    expect(new ActivityTemplate()).toBeTruthy();
  });
});

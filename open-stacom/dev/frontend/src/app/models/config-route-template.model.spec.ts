import { ConfigRouteTemplate } from './config-route-template.model';

describe('ConfigRouteTemplate', () => {
  it('should create an instance', () => {
    expect(new ConfigRouteTemplate()).toBeTruthy();
  });
});

import { PreviousEditionForm } from './previous-edition-form.model';

describe('PreviousEditionForm', () => {
  it('should create an instance', () => {
    expect(new PreviousEditionForm()).toBeTruthy();
  });
});

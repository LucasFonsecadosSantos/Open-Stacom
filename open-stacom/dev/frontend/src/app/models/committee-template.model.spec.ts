import { CommitteeTemplate } from './committee-template.model';

describe('CommitteeTemplate', () => {
  it('should create an instance', () => {
    expect(new CommitteeTemplate()).toBeTruthy();
  });
});

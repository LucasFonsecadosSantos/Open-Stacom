import { ProceedingTemplate } from './proceeding-template.model';

describe('ProceedingTemplate', () => {
  it('should create an instance', () => {
    expect(new ProceedingTemplate()).toBeTruthy();
  });
});

import { PricePlanTemplate } from './price-plan-template.model';

describe('PricePlanTemplate', () => {
  it('should create an instance', () => {
    expect(new PricePlanTemplate()).toBeTruthy();
  });
});

import { PastEditionTemplate } from './past-edition-template.model';

describe('PastEditionTemplate', () => {
  it('should create an instance', () => {
    expect(new PastEditionTemplate()).toBeTruthy();
  });
});
